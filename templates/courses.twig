{% for post in posts %}
    <article>
        <h2>{{ post.title }}</h2>
        <p>{{ post.content }}</p>
        <button onclick="openPopup('{{ post.title }}')">Подати заявку</button>
    </article>
{% endfor %}
